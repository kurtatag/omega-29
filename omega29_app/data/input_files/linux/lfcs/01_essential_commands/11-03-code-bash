sudo su
# become root (will require a root password)

sudo -s
# to switch to a shell with root's privileges
# -s, --shell  = run your own (the invoking user's) shell

sudo -i
# to switch to a shell with root's privileges
# -i, --login  = run the target user's shell and switch into their
#                home directory