git push <remote_repo_alias> <local_branch>

git push origin master
# Send commits from a local branch 'master' to the branch called the same on
# a remote repository 'origin'.
# If master did not exist remotely, it would be created.

git push -u origin master
# -u (or --set-upstream)
#    if the current push succeeds, make the local branch 'master' track its
#    remote counterpart

git push
# pushes the current branch to its remote counterpart
# (if tracking is configured)